{
  "name": "threat-model-tool-hugo-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "hugo",
    "serve": "hugo server --bind 0.0.0.0 --port 4323"
  },
  "devDependencies": {
    "hugo-bin": "^0.145.0"
  }
}