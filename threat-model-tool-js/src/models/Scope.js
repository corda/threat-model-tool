import BaseThreatModelObject from './BaseThreatModelObject.js';

class Scope extends BaseThreatModelObject {
    constructor(dictData, parent) {
        super(dictData, parent);
    }
}

export default Scope;
