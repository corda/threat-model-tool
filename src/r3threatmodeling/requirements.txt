# install with:
# pip install -r requirements.txt

# Core tree functionality (extracted package)
tree-node>=1.0.0

# Data processing and validation
pathvalidate
pyyaml
ruamel.yaml
semantic-version

# Documentation and reporting
markdown==3.7
mako
markdown-include
mkdocs

# Security and assessment
cvss

# Development and utilities
watchdog
pyvis
importlib_resources
jira

# Note: shutils removed as it's not a valid package (shutil is built-in)
